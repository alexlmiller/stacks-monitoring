# Stacks PoX Exporter Configuration
# Copy to /etc/default/pox-exporter

# Stacks node RPC endpoint
STACKS_NODE_URL=http://localhost:20443

# Port to expose metrics on
POX_EXPORTER_PORT=9816

# Address to bind the exporter to
POX_EXPORTER_LISTEN_ADDRESS=127.0.0.1
